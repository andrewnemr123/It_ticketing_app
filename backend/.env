# Local dev config (git-ignored). Copy of .env.example with working defaults.
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# MAMP MySQL (started via the MAMP app). MAMP default: port 8889, root/root.
DB_HOST=127.0.0.1
DB_PORT=8889
DB_USER=root
DB_PASSWORD=root
DB_NAME=it_ticketing

JWT_SECRET=dev_only_secret_please_change_in_production_0a1b2c3d4e5f
JWT_EXPIRES_IN=8h

UPLOAD_DIR=uploads
MAX_UPLOAD_BYTES=5242880
